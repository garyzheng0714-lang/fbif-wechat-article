# 项目规则：fbif-wechat-article

## 🔴 动手前先看这里：三系统边界（2026-08-06 用户裁定）

FBIF 内容链路由**三个互相独立的系统**组成，各有各的代码库、数据库和服务器。**唯一的通信方式是 HTTP API，不共享任何存储。**

| 系统 | 干什么 | 时效 | 数据库 | 域名 |
| --- | --- | --- | --- | --- |
| **本项目**（fbif-wechat-article） | **归档监控**：微信公众号官方 API 归档、阅读/粉丝指标 | **T+1**，每天 09:00 CST 一次 | SQLite | `112.124.103.65:3002` |
| FoodTalks_Feed | **实时监控**：RSS/供应商资讯采集、分类、判断、筛选 | **实时**（持续轮询） | PostgreSQL（它自己的） | `feed.foodtalks.cn` |
| FBIF公众号排版 | **加工**：排版、风险检查、飞书审核、分发到公众号/FoodTalks | 收到即处理 | JSON 文件（它自己的） | `fbifmp-layout.foodtalks.cn` |

**本项目的定位一句话：只做监控。** 用官方 API 归档自家公众号的文章与数据，并把普通图文的「链接 + 分类证据」推给排版工具，到此为止。

### 本项目是 T+1，不需要高频轮询

微信官方 API **只拿得到上一天发布的文章**，所以每天同步一次昨天的量就够（`sync/scheduler.go` 的 daily cron，09:00 CST）。**不要改成高频轮询**——拿不到更新的数据，只会烧掉每日额度。

当天要看到公众号新文，靠的是 `FoodTalks_Feed` 经今天看啥接口拿的 RSS，那条线才是实时的。两条线时效不同，别混为一谈。

`AUTO_LAYOUT_POLL_INTERVAL_MINUTES=15` 是**投递轮询**——扫本地库找待投递文章，不调微信 API、不耗额度，与采集频率无关。

两个监控端 → `POST /api/publish/site-sync` → 排版工具。**载荷只有链接**，不含正文、标题、来源、作者、封面（排版侧对这五个字段一律 400）。本项目已于提交 `5f036db` 收敛到位，`autolayout.Article` 只有 url 加三个类型证据字段，**不得回退**。

### 三条不得跨越的线

1. **不碰排版工具的代码库。** 排版规则、卡片长什么样、他们的部署和告警文案，都不是本项目该改的。看出问题就说出来，由他们改。反过来他们也不该改本项目。
2. **不给排版工具发正文。** 传了正文，排版侧就跳过自己的抓取器，微信小标题识别、图注关键词识别、导流尾剥离全部失效。2026-08-06 生产任务 #72 因此产出 4.7 万字长文零小标题；同一篇原文实测：传正文 0 个小标题，只传链接 7 个。
3. **监控职责归监控端。** 服务健康探针、告警发送、失败计数由监控端持有，不寄放在被监控方那里。反例：排版工具仓库的 `monitoring/watchdog.py` 曾探本项目的 `/api/wechat/official/monitoring`，两处都配错——3002 端口公网不通、探针统一发 `X-Publish-Sync-Token` 而本项目认的是 `API_KEY`——连续失败 715 次没人发现，因为不在负责人手上。本项目对外只需保证被探的健康端点可用，不主动探别人。

## 项目边界

- 本项目采集微信公众号官方 API 数据，持久化原始响应与规范化数据，并在门禁允许时同步到飞书多维表格。
- 本地环境的 IP 不在微信 API 白名单；涉及真实微信数据的同步、回填和验收必须在受控远程环境执行。
- 凭据只从环境变量或服务器密钥文件读取，不得写入代码、文档、日志或测试夹具。

## 已发布文章真值红线

- `freepublish/batchget.total_count` 是发布对象数，不是历史文章数。
- 当前观察到的 273 个发布对象和拆出的 311 条文章记录，都不能称为公众号历史全量。
- 只有完成多接口原始响应留存、分页/时间覆盖、稳定标识关联、去重、缺口审计，并经用户确认后，才可把状态标为“历史全量已核验”。
- 未完成上述审计时，接口、日志、文档和 UI 必须明确写“未完成/未核验”。
- 飞书批量同步在全量审计完成并经用户确认前保持关闭；素材库不是已发布文章真相源。
- 详细合同见 `docs/PUBLISHED-ARTICLE-TRUTH-CONTRACT.md`，自动守护见 `archive/store_test.go`。

## 开发与验证

```bash
go test ./...
go run .
```

- 修改采集、关联、计数或同步逻辑时，先补能复现口径错误的测试。
- 完成前检查 README、HTTP API、日志和状态字段是否使用准确的数据源名称与覆盖范围。
- 不以单表行数、单接口游标完成或单次快照代替跨源完整性证明。

## 文档地图

- `README.md`：运行、配置、API 与部署入口。
- `docs/PUBLISHED-ARTICLE-TRUTH-CONTRACT.md`：已发布文章统计的最高优先级口径。
- `AGENTS.md`：本文件的软链接，不维护第二份规则。

任何任务都以“用户真实可用”而不是“代码已写完、测试已通过”为完成标准：动手前先核对现有实现、真实数据、业务语义和用户提供的截图或范例，优先复用成熟开源方案、官方组件及项目内已经验证的模式，不得擅自增删功能、改变含义或重复造轮子；实现必须从底层解决问题，不用遮罩、图层补丁、假数据、假按钮或程序化挪动旧素材掩盖缺陷，保存、查询、编辑、拖动、排序、刷新恢复、权限、空态、长文本和响应式等都要形成真实闭环；验收时必须在真实页面用真实数据逐项操作，亲眼检查基线、圆角、层级、溢出、动效和操作反馈，并在修复后回归原有数据与主流程，不能只凭测试通过就宣称完成。用户给出成熟参考时，先准确还原其信息结构与交互，再做有依据的优化；所有未完成、未验证和受阻项必须如实说明，只有确认不存在假成功、修一处坏一处和低级视觉错误后，才能说任务完成。
