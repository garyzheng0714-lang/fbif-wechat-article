# 交接：投递队列 55 篇卡在「未分类」（2026-08-06）

来自上游 `FoodTalks_Feed`（资讯监控端）的会话。**本文只做只读排查，没有改本仓库任何代码。**

## 现象

`GET /api/wechat/official/status` 里：

```
layout.outbox.delivered          2
layout.outbox.skippedNewspic     4
layout.outbox.heldUnclassified  55   ← 卡住
```

`heldUnclassified > 0` 会让 `official` 探针的 `ready` 一直是 false（`autolayout/dispatcher.go:320`）。

## 根因：微信已发布接口不返回文章类型

`freepublish` 拿回来的文章，`article_type` **全是空的**：

| 数据来源 | 有类型 / 总数 |
| --- | --- |
| `freepublish`（已发布） | **0 / 367** |
| `draft`（草稿箱） | 95 / 95 |
| `material_news`（素材库） | 35 / 35 |

`archive/layout.go` 的候选查询因此只能**反向推断**——拿已发布文章去草稿箱找一条长得一样的，借它的类型。匹配条件是标题 + 位置 + 作者 + 原文链接 + 封面 media_id + 封面 URL 全等，且草稿更新时间落在发布时间前 30 天内。

匹配不上就是空字符串，落进 `dispatcher.go:362` 的 `default` 分支被 `heldUnclassified++` 挡下。

**挡下本身是对的**（fail-closed，宁可不发也不能把图集当普通图文投出去）。问题是草稿箱会被清理——发得越久的文章越容易断线索，所以 367 篇里只有 130 篇配上了类型。

## 存量分析：两组界线很干净

对 366 篇有正文的空类型文章做特征统计（`content_html` 字节数）：

| 组 | 篇数 | 最短 | 平均 | 最长 |
| --- | --- | --- | --- | --- |
| 已知图集 `newspic` | 12 | 270 | **875** | 1,955 |
| **空类型 · 正文无 `<img>`** | **194** | 59 | **727** | 3,738 |
| **空类型 · 正文有 `<img>`** | **172** | 481 | **107,034** | 2,588,737 |
| 已知图文 `news` | 122 | 6 | **63,177** | 199,395 |

两组各自紧贴一个已知类型，平均值差 **147 倍**。

补充证据：

- 已知 `newspic` **13 / 13 全部**在 `raw_json` 里带 `image_info` 字段；已知 `news` **0 / 122** 有该字段。这是微信官方的结构差异（图集图片放在 `image_info`，普通图文没有这个字段），不是数据巧合。
- 那 194 篇里 `<img>`、`data-src`、`mmbiz` 图床域名**一个都没有**——正文里根本没有图片，与图集正文形态一致。
- 已知 `news` 里正文短于 3000 字节的只有 3 篇（异常值）。

## 建议的判据

按可靠性排序，建议组合使用：

1. **`raw_json` 含 `image_info` → `newspic`**（现有样本 13/13 命中、0 误判，且有官方字段语义支撑）
2. **正文含 `<img>` 且长度 > 3000 → `news`**（已知 `news` 122 篇中 117 篇有 `<img>`；已知 `newspic` 0 篇有）
3. **两条都不满足 → 保持现在的 fail-closed**，不要猜

按这个判据，172 篇「有 img」的可以放行，194 篇「无 img」应确认为图集（本来就不该投递）。

**样本量提醒**：已知 `newspic` 只有 13 篇。判据一有官方字段语义支撑，但仍建议先 dry-run 打印判定结果、人工抽查十几篇再启用，不要直接开闸。

## 长期解法

`publishcallback/service.go` 里已经有 `OriginalArticleType` 字段（群发回调会带类型），是比「翻草稿箱」更可靠的类型来源。但 `official_publish_event_articles` 表目前 **0 条**——回调没启用或没收到数据。

启用回调后，新发的文章能拿到权威类型，不再依赖草稿箱残留。**历史文章仍需上面的判据或人工标注。**

## 归属说明

这是本仓库的活。上游只做了只读排查，把结论交过来，没有改代码、没有改数据。

三系统边界（2026-08-06 用户裁定）见本仓库 `CLAUDE.md` 第一屏：监控端不碰对方代码库，看出问题就说出来，由对方改。

---

## 附：本项目是 T+1，不需要高频轮询（2026-08-06 用户澄清）

微信官方 API **只拿得到上一天发布的文章**。现有实现已经是对的——`sync/scheduler.go` 每天 09:00 CST 跑一次 daily cron，不是高频轮询。**不要改成高频**：拿不到更新的数据，只会烧掉每日额度。

容易混淆的一点：env 里的 `AUTO_LAYOUT_POLL_INTERVAL_MINUTES=15` 是**投递轮询**——扫本地 SQLite 找待投递文章，不调微信 API、不耗额度，与采集频率无关。

当天要看到公众号新文，靠的是 `FoodTalks_Feed` 经今天看啥接口拿的 RSS，那条线才是实时的。同一篇文章可能先后从两条线进入排版工具，`source_key` 幂等保证只建一单。

这段已同步写入三个项目 `CLAUDE.md` 第一屏的边界表（新增「时效」列）。
